# Artifacts

This folder is used by the notebook/scripts to store outputs such as:
- trained model pipeline (joblib)
- metrics (json)
- selected threshold (json)

Artifacts are not committed by default.
